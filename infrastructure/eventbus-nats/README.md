Minimal in-memory EventBus crate used for local tests.

Provides:
- `Event` struct
- `EventBus` trait
- `InMemoryEventBus` test implementation with `publish`, `subscribe`, and `next()` helper

This crate is a lightweight test helper and is not intended to be a full NATS client implementation.