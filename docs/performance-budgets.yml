# Performance budgets used by CI and monitoring
api_gateway:
  p50_latency: "50ms"
  p95_latency: "200ms"
  p99_latency: "500ms"

event_bus:
  publish_latency: "5ms"
  delivery_latency: "50ms"

desktop_ui:
  initial_load: "1500ms"
  interaction: "100ms"
  memory: "200MB"

adapters:
  rsi_auth: "2000ms"
  game_log_parse: "100ms"

notes: |
  These budgets are enforced by CI checks and monitoring. Values are performance targets and should be validated by benchmarks/bench jobs.