# sc_manager_adapters

Adapter crates are intentionally thin and only call external APIs. Core must remain free of any adapter-specific code.

This folder contains stubs for: `game_log`, `rsi`, `fleetyards`, `erkul`.
