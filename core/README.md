# sc_manager_core

Core domain crate. Contains domain entities, value objects and domain events. Must remain free of adapter or UI-specific logic.

See `src/domain`, `src/value_objects`, `src/events` for implementation stubs.
