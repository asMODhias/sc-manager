# Domain

Contains core domain entities (Organization, Member, Fleet, Ship, Equipment, Event, Role, Permission, Session, GameEvent).

Follow rules in `docs/04_DEV_GUIDE_COPILOT.md` when expanding.
